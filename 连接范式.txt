连接范式：

1. 从连接池申请连接，超过负载连接数则返回503后断开，超过最大连接数则阻塞
2. 按行读取从客户端接收Http请求头，写死按CRLF读取，读取中出现错误则关闭连接
3. 每接收一行，即解析一行，一旦发现不可解析的行，停止接收，按illegal_data.json中的执行，如果被禁用则返回http400
4. 根据Context中的定义，在其他规则中筛选出已开启的规则，分别实例化它们的ruler，逐一过滤
